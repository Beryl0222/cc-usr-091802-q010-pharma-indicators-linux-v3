# 医药规划指标证据库

本项目支撑医药工业五年规划的跨部门指标监测。指标定义、计算口径、责任来源、明细证据和发布快照相互关联，迟报与修订只影响依赖它们的后续计算。

`fixtures/sample.json` 列出若干 2030 年预期指标及其单位，包括行业营业收入、首创新药比例、创新器械数量和重点企业数量。样例只用于说明口径，不包含企业经营数据。

执行 `python3 service.py --check` 可检查基础入口，`python3 -m unittest -v` 会核对服务身份和指标单位。HTTP 服务的健康地址是 `/health`。
